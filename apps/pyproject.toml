[project]
name = "ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chang Gu", email = "justforsb@outlook.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openai>=3.10.0",
]

[project.scripts]
ai = "ai:main"

[build-system]
requires = ["uv_build>=0.11.32,<0.12.0"]
build-backend = "uv_build"
